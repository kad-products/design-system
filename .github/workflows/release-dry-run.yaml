name: Release Dry Run

on:
  pull_request:
    branches: [main]

jobs:
  release-dry-run:
    uses: kad-products/platform/.github/workflows/release-dry-run.yaml@better-shared-workflows
    permissions:
      contents: read
    secrets: inherit
